---
тип: инфраструктура
обновлено: 2026-05-06
источники: уроки 11, 16; SESSION_BRIEF
---

# Инфраструктура — компактный справочник

## Биржа и данные

### MOEX ISS API (данные)
- **URL:** https://iss.moex.com/iss/
- **Стоимость:** бесплатно
- **Что даёт:** цены, стакан, история, параметры инструментов
- **Тикеры:** `TATN` (обычка), `TATNP` (привилегированные)
- **Секция:** ММВБ (TQBR)
- **Документация:** TODO — найти и зафиксировать ссылку
- **Rate limit:** TODO — уточнить

### Данные реального времени (для бота)
- **Вариант 1:** MOEX ISS polling (бесплатно, ~500ms задержка)
- **Вариант 2:** MOEX Market Data (платно, низкая задержка)
- **Решение для фазы 1–2:** ISS polling (достаточно для среднесрочки)

---

## Брокер и исполнение

### CScalp / KasCapital (основной)
- **Терминал:** Привод (специализированный стаканник)
- **Регистрация:** https://kas.wtf/referal/KlevcovDIV
- **Поддержка:** @KasCapital_Robot (Telegram)
- **Тип счёта:** проп-счёт
- **API для автоматизации:** TODO — уточнить наличие и документацию
- **Горячие клавиши:** Escape = отменить все лимитные заявки

### Диверсификация брокеров (из урок_16)
- Несколько пропов = правильно (если маржит в одном — перекидываешь)
- Разные пропы = разные таблицы переносов, разные лимиты
- ЗАПРЕТ: не торговать одновременно через разные счета одного брокера по одному инструменту

---

## Технический стек (планируемый)

### Фаза 1 — Скринер

```
Python 3.11+
├── httpx / aiohttp      ← async HTTP для MOEX ISS API
├── asyncio              ← вся логика через asyncio (скорость критична)
├── pandas               ← обработка временных рядов спреда
├── python-telegram-bot  ← Telegram-алерты
└── SQLite / PostgreSQL  ← хранение истории спреда
```

### Фаза 3–4 — Полный бот

```
Python 3.11+
├── asyncio + uvloop     ← event loop (скорость)
├── MOEX ISS API         ← данные
├── Broker API           ← исполнение (TODO: уточнить по брокеру)
├── Redis                ← state management (позиции, ордера)
├── PostgreSQL           ← история сделок, логи
├── Prometheus + Grafana ← мониторинг
├── Telegram Bot         ← алерты + ручное управление
└── Docker               ← деплой
```

---

## Переменные окружения (.env — НЕ коммитить)

```bash
# Брокер
BROKER_API_KEY=
BROKER_API_SECRET=
BROKER_BASE_URL=

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Режим
DRY_RUN=true              # ВСЕГДА true по умолчанию

# Параметры риска (переопределяют defaults)
MARGIN_WARNING_THRESHOLD=0.50
MARGIN_CRITICAL_THRESHOLD=0.65
MAX_DAILY_LOSS_PCT=0.03
```

---

## Мониторинг и алерты

### Что мониторить (TODO: реализовать)

| Метрика | Порог | Алерт |
|--------|-------|-------|
| Спред TATN/TATNP | crossing 2.5 руб | Telegram |
| Маржа | > 50% | Telegram warning |
| Маржа | > 65% | Telegram CRITICAL + auto-close |
| API недоступен | > 10 сек | Telegram |
| Дисбаланс ног | > 50 лотов | Telegram |
| Суточный P&L | < −3% | Telegram + стоп торговли |

### TradingView (ручная фаза)
- График спреда: `TATN - TATNP` или `TATN / TATNP`
- Алерт уровень: crossing 2.5 руб → уведомление на телефон/часы
- Session breaks включить (визуализация вечерней сессии)

---

## Среда выполнения

### Требования к latency
- Для среднесрочки ТАТки: latency не критична (минуты/часы горизонт)
- Для скальпинга: важна, но co-location не нужен (в отличие от HFT)
- DRY_RUN=true по умолчанию везде до явного разрешения

### Рекомендованное окружение (производство)
- VPS в России (минимизировать задержку до MOEX)
- Или локальная машина с надёжным интернетом
- UPS / резервное интернет-соединение (для production фазы)

---

## Ключевые ссылки (TODO — заполнять по мере изучения)

| Ресурс | URL | Назначение |
|--------|-----|-----------|
| MOEX ISS API | TODO | Данные |
| CScalp API | TODO | Исполнение |
| MOEX торговый календарь | TODO | Расписание ТО, праздники |
| MOEX технические ошибки | TODO | Статусная страница |
